# 🎯 Финальная презентация — outline для сверки

> **АРТЁМ, ШАЛЫМИНОВ — читать сначала!**
>
> Если в твоём pptx содержимое слайдов 5, 6, 9, 11 **НЕ совпадает** с
> блоками ниже — у тебя **СТАРЫЙ файл**. Открой `docs/HR_AI_Agent_FINAL_4june.pptx`,
> а не `HR_AI_Agent_clean_revised.pptx` и не `HR_AI_Agent_revised_v2.pptx`.

---

## ✅ Правильный файл — что должно быть в нём

### Слайд 5 «Предложенное решение» → блок «Недостатки MVP»

Если видишь это — **файл правильный**:
```
• ограниченный пул внешних API
• локальное JSON-состояние
• ручной запуск цикла
• датасет разметки на стадии расширения
```

Если видишь это — **файл СТАРЫЙ**, открой другой:
```
• ограниченный пул источников
• локальное JSON-состояние
• ручной запуск цикла
• часть данных используется для демонстрации   ← это устарело
```

---

### Слайд 6 «Методология источников» → блок «Курируемый пул»

Если видишь это — **файл правильный**:
> «Каталог содержит **26 источников: 14 активных и 12 кандидатов** на
> A/B-тестирование. У каждого зафиксированы quality_score,
> topics_covered, region и обоснование выбора rationale.»

Если видишь это — **файл СТАРЫЙ**:
> «Текущий MVP использует реальные RSS-источники и демо-данные.
> Целевой пул для следующего этапа — 20–30 HR-релевантных источников…»

---

### Слайд 9 «Доработки за итерацию» → все 5 карточек

Если в новой версии — **все 5 карточек ДОЛЖНЫ быть такими**:

1. **Тесты и CI** → «109 автотестов проходят зелёными, CI на GitHub
   Actions с матрицей Python 3.10 / 3.11, Ruff lint и pytest-coverage.»
2. **Авторегуляция пула** → «Модуль source_manager отслеживает
   duplicate_rate и активность источников и автоматически рекомендует
   деактивацию шумных и активацию ценных кандидатов.»
3. **Аналитика источников** → «source_analytics считает per-source
   метрики: items_fetched, duplicate_rate, avg_risk_score,
   topic_coverage, unique_contribution — реальные цифры в дашборде.»
4. **SimHash-дедупликация** → «Двухуровневая фильтрация: URL-уровень
   и контентный SimHash. Самописная имплементация без внешних
   зависимостей, видны кросс-источниковые дубли.»
5. **Качество классификации** → «Размеченный датасет
   labeled_news.json (35 новостей, 7 HR-тем, RU/EN). По нему считается
   Cohen's Kappa между разметчиками — верхняя граница качества модели.»

Если в твоей версии карточки **«MVP-сценарий / Роли и персонализация /
Методология источников / Персистентное состояние / Защита MVP»** —
у тебя СТАРЫЙ файл.

---

### Слайд 11 «Репозиторий проекта» → блок «Структура проекта»

Если видишь это — **файл правильный**:
```
hr-ai-agent/
├─ config/
│  ├─ settings.py
│  └─ sources_catalog.py   ← 26 источников
├─ dashboard/app.py
├─ src/
│  ├─ agent/
│  ├─ processing/deduplicator.py  ← SimHash
│  ├─ sources/source_manager.py   ← авторегуляция
│  └─ sources/source_analytics.py ← per-source метрики
├─ tests/                  ← 109 тестов
│  └─ fixtures/labeled_news.json
├─ docs/
│  ├─ METHODOLOGY.md
│  └─ defense_script_mvp.md
├─ scripts/demo_start.{sh,ps1}
├─ .github/workflows/tests.yml
├─ Dockerfile + docker-compose.yml
└─ requirements.txt + requirements-dev.txt
```

Если видишь короткое дерево без `tests/`, `docs/`, `scripts/`,
`.github/`, `Dockerfile` — у тебя СТАРЫЙ файл.

---

## 📁 Какой файл открывать

**ТОЛЬКО ЭТОТ:**
```
C:\Users\Thunderobot\Desktop\hr_agent\docs\HR_AI_Agent_FINAL_4june.pptx
```

Все остальные файлы pptx — **устаревшие**, не трогать:
- ~~`HR_AI_Agent_clean_revised.pptx`~~ (из Telegram Desktop) — старая версия Шалыминова до правок Клода
- ~~`HR_AI_Agent_revised_v2.pptx`~~ — промежуточная версия, оставлена для истории

## 🔧 Что ещё надо сделать с правильным файлом

1. Открыть в PowerPoint
2. Пройтись по 11 слайдам — поправить визуал, если что-то съехало после
   правок через python-pptx (шрифт, выравнивание)
3. Когда Беспамятных/Егоров пришлют ответы Google Form — добавить
   **слайд 12 «Обратная связь пользователей»** (Клод подготовит макет
   с цитатами и средней оценкой)
4. Экспортировать **запасной PDF** на флешку (на случай если PowerPoint
   у комиссии затупит)
5. Когда Коновалов пришлёт MP4 видео — вставить через
   **Вставка → Видео → С компьютера** в слайд 6 «Демонстрация
   работающего решения»
